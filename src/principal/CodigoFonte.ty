main
    number variavel;
    number valorLido;

    variavel = 5;

    input (valorLido);

    if(variavel < 5)
        output("eh menor que 5");
    else
        if(variavel > 5)
            output("eh maior que 5");
        else
            output("eh igual a 5");
        endif
    endif

    while(valorLido < variavel)
        output(valorLido);
        valorLido = valorLido + 1;
    endwhile

endmain